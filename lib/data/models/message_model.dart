class MessageModel {
  String idConversation, message, idSended;
  DateTime dateTime;

  MessageModel({
    required this.idConversation,
    required this.message,
    required this.idSended,
    required this.dateTime,
  });
}
